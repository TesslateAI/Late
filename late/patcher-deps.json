{
  "comment": "Define the Python packages to be installed by the 'late patch' command.",
  "build_from_source": {
    "comment": "Dependencies needed only when building packages like Flash Attention from source.",
    "packages": [
      "ninja",
      "cmake",
      "wheel",
      "pybind11"
    ]
  },
  "pytorch_rocm": {
    "comment": "PyTorch ROCm installation - MUST use ROCm-specific wheels to avoid CUDA version",
    "nightly_index_url": "https://download.pytorch.org/whl/nightly/rocm6.4/",
    "stable_index_url": "https://download.pytorch.org/whl/rocm6.2/",
    "packages": ["torch", "torchvision", "torchaudio"]
  },
  "machine_learning": {
    "comment": "Core ML libraries that depend on PyTorch - installed AFTER PyTorch",
    "packages": [
      "transformers>=4.56.2",
      "datasets",
      "accelerate",
      "trl",
      "peft",
      "wandb",
      "scipy",
      "numpy"
    ]
  }
}